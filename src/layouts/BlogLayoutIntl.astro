---
import BaseHead from "../components/BaseHead.astro";
import NavigationIntl from "../components/global/NavigationIntl.astro";
import FooterIntl from "../components/global/FooterIntl.astro";
import GoogleAnalytics from "../components/global/GoogleAnalytics.astro";
import Analytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";
import { stripLocale, localizePath, localesFor, type Locale } from "../i18n/config";

interface Props {
  locale: Locale;
  title: string;
  description: string;
  publishDate: string;
  modifiedDate?: string;
  author?: string;
  readTime?: string;
  tags?: string[];
  canonical?: string;
  ogImage?: string;
  faqSchema?: Array<{question: string; answer: string}>;
}

const {
  locale,
  title,
  description,
  publishDate,
  modifiedDate,
  author = "AI Security",
  readTime = "5 min",
  tags = [],
  canonical,
  ogImage = "https://aisecurity.es/og-image.png",
  faqSchema
} = Astro.props;

const fullTitle = `${title} | AI Security`;

// hreflang recíproco: todas las versiones de este slug
const canonicalPath = stripLocale(Astro.url.pathname);
const esAltUrl = new URL(canonicalPath, Astro.site).href;
const alternates = localesFor(canonicalPath).map((l) => ({
  locale: l,
  href: new URL(localizePath(canonicalPath, l), Astro.site).href,
}));

const home = localizePath("/", locale);
const blog = localizePath("/blog", locale);

const L = {
  fr: {
    home: "Accueil", blog: "Blog", article: "Article", back: "Retour au blog",
    read: "de lecture", copy: "📋 Copier le code", copied: "✓ Copié !",
    dateLocale: "fr-FR",
  },
  nl: {
    home: "Home", blog: "Blog", article: "Artikel", back: "Terug naar blog",
    read: "leestijd", copy: "📋 Code kopiëren", copied: "✓ Gekopieerd!",
    dateLocale: "nl-NL",
  },
  en: {
    home: "Home", blog: "Blog", article: "Article", back: "Back to blog",
    read: "read", copy: "📋 Copy code", copied: "✓ Copied!",
    dateLocale: "en-US",
  },
} as const;
const s = (L as Record<string, (typeof L)["en"]>)[locale] ?? L.en;
const formattedDate = new Date(publishDate).toLocaleDateString(s.dateLocale, { year: 'numeric', month: 'long', day: 'numeric' });
---

<html
  lang={locale}
  class="scroll-smooth selection:bg-accent-500 selection:text-white no-touchevents hydrated">
  <head>
    <GoogleAnalytics />
    <BaseHead
      title={fullTitle}
      description={description}
      canonical={canonical}
      ogImage={ogImage}
    />
    {alternates.map((a) => (
      <link rel="alternate" hreflang={a.locale} href={a.href} />
    ))}
    <link rel="alternate" hreflang="x-default" href={esAltUrl} />
    <!-- Article Schema.org for SEO -->
    <script is:inline type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Article",
      "inLanguage": locale,
      "headline": title,
      "description": description,
      "author": {
        "@type": "Organization",
        "name": author,
        "url": "https://aisecurity.es"
      },
      "publisher": {
        "@type": "Organization",
        "name": "AI Security",
        "logo": {
          "@type": "ImageObject",
          "url": "https://aisecurity.es/favicon.png"
        }
      },
      "datePublished": publishDate,
      "dateModified": modifiedDate || publishDate,
      "mainEntityOfPage": {
        "@type": "WebPage",
        "@id": canonical
      },
      "image": ogImage,
      "keywords": tags.join(", ")
    })} />
    {faqSchema && faqSchema.length > 0 && (
      <script is:inline type="application/ld+json" set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "inLanguage": locale,
        "mainEntity": faqSchema.map((faq: {question: string; answer: string}) => ({
          "@type": "Question",
          "name": faq.question,
          "acceptedAnswer": {
            "@type": "Answer",
            "text": faq.answer
          }
        }))
      })} />
    )}
  </head>
  <body class="flex flex-col min-h-svh">
    <NavigationIntl />

    <main class="grow">
      <!-- Article Header -->
      <section class="py-16 relative overflow-hidden" style="background:#050f1f;border-bottom:1px solid rgba(255,255,255,0.08);">
        <canvas id="blog-hero-canvas" class="absolute inset-0 w-full h-full" style="opacity:0.45;"></canvas>
        <div class="absolute inset-x-0 bottom-0 h-12" style="background:linear-gradient(to bottom,transparent,#020817);"></div>

        <div class="relative z-10 max-w-4xl mx-auto px-6 md:px-8">
          <!-- Breadcrumb -->
          <nav class="mb-6" aria-label="Breadcrumb">
            <ol class="flex items-center gap-2 text-sm text-white/70">
              <li><a href={home} class="hover:text-white transition-colors">{s.home}</a></li>
              <li aria-hidden="true">→</li>
              <li><a href={blog} class="hover:text-white transition-colors">{s.blog}</a></li>
              <li aria-hidden="true">→</li>
              <li class="text-white font-medium" aria-current="page">{s.article}</li>
            </ol>
          </nav>

          <!-- Tags -->
          {tags.length > 0 && (
            <div class="flex flex-wrap gap-2 mb-4">
              {tags.map(tag => (
                <span class="px-3 py-1 bg-white/20 backdrop-blur-sm text-white text-xs font-semibold rounded-full border border-white/30">
                  {tag}
                </span>
              ))}
            </div>
          )}

          <!-- Title -->
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4 leading-tight">
            {title}
          </h1>

          <!-- Description -->
          <p class="text-lg md:text-xl text-white/90 mb-6 leading-relaxed">
            {description}
          </p>

          <!-- Meta info -->
          <div class="flex flex-wrap items-center gap-4 text-sm text-white/80">
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
              <span>{author}</span>
            </div>
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
              <time datetime={publishDate}>{formattedDate}</time>
            </div>
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <span>{readTime} {s.read}</span>
            </div>
          </div>
        </div>
      </section>

      <!-- Article Content -->
      <article class="py-12 md:py-16 relative overflow-hidden" style="background-color: #010303;">
        <div class="absolute inset-0 opacity-15">
          <img src="/images/3409297.jpg" alt="Background" class="w-full h-full object-cover" />
        </div>

        <div class="relative z-10 max-w-4xl mx-auto px-6 md:px-8">
          <div class="prose prose-lg prose-base max-w-none
            prose-headings:font-bold prose-headings:text-white
            prose-h2:text-2xl prose-h2:mt-8 prose-h2:mb-4 prose-h2:border-b prose-h2:border-base-700 prose-h2:pb-3
            prose-h3:text-xl prose-h3:mt-6 prose-h3:mb-3
            prose-p:text-white/90 prose-p:leading-relaxed prose-p:mb-4
            prose-a:text-accent-400 prose-a:no-underline hover:prose-a:underline
            prose-strong:text-white prose-strong:font-semibold
            prose-code:bg-black prose-code:px-2 prose-code:py-1 prose-code:rounded prose-code:text-sm prose-code:font-mono prose-code:before:content-none prose-code:after:content-none prose-code:border prose-code:border-emerald-500/30
            prose-pre:bg-black prose-pre:rounded-xl prose-pre:border-2 prose-pre:border-blue-500/30 prose-pre:shadow-lg prose-pre:shadow-blue-500/10
            prose-ul:my-4 prose-li:text-white/90 prose-li:my-2
            prose-ol:my-4
            prose-img:rounded-xl prose-img:shadow-lg prose-img:border prose-img:border-base-700">
            <slot />
          </div>
        </div>
      </article>

      <!-- Back to Blog -->
      <section class="py-8 border-t border-base-700 relative overflow-hidden" style="background-color: #121416;">
        <div class="absolute inset-0 opacity-15">
          <img src="/images/3409297.jpg" alt="Background" class="w-full h-full object-cover" />
        </div>

        <div class="relative z-10 max-w-4xl mx-auto px-6 md:px-8">
          <a
            href={blog}
            class="inline-flex items-center gap-2 text-accent-400 hover:text-accent-300 font-medium transition-colors">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            {s.back}
          </a>
        </div>
      </section>
    </main>

    <FooterIntl locale={locale} />
    <Analytics />
    <SpeedInsights />
  </body>
</html>

<script is:inline>
(function() {
  const canvas = document.getElementById('blog-hero-canvas');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  function resize() {
    canvas.width = canvas.offsetWidth;
    canvas.height = canvas.offsetHeight;
  }
  resize();
  window.addEventListener('resize', resize);
  const NODES = 60, MAX_DIST = 140;
  const nodes = Array.from({ length: NODES }, () => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height,
    vx: (Math.random() - 0.5) * 0.4,
    vy: (Math.random() - 0.5) * 0.4,
    r: Math.random() * 1.5 + 1,
    phase: Math.random() * Math.PI * 2,
  }));
  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    nodes.forEach(n => {
      n.x += n.vx; n.y += n.vy; n.phase += 0.02;
      if (n.x < 0 || n.x > canvas.width) n.vx *= -1;
      if (n.y < 0 || n.y > canvas.height) n.vy *= -1;
    });
    for (let i = 0; i < NODES; i++) {
      for (let j = i + 1; j < NODES; j++) {
        const dx = nodes[i].x - nodes[j].x, dy = nodes[i].y - nodes[j].y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < MAX_DIST) {
          ctx.beginPath();
          ctx.moveTo(nodes[i].x, nodes[i].y);
          ctx.lineTo(nodes[j].x, nodes[j].y);
          ctx.strokeStyle = `rgba(96,165,250,${(1 - dist/MAX_DIST)*0.25})`;
          ctx.lineWidth = 0.8;
          ctx.stroke();
        }
      }
    }
    nodes.forEach(n => {
      const pulse = 0.5 + 0.5 * Math.sin(n.phase);
      ctx.beginPath();
      ctx.arc(n.x, n.y, n.r + pulse * 0.8, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(129,140,248,${0.4 + pulse * 0.4})`;
      ctx.fill();
    });
    requestAnimationFrame(draw);
  }
  draw();
})();
</script>

<script is:inline define:vars={{ copyLabel: s.copy, copiedLabel: s.copied }}>
  document.addEventListener('DOMContentLoaded', () => {
    const codeBlocks = document.querySelectorAll('article pre');
    codeBlocks.forEach((block) => {
      const codeElement = block.querySelector('code');
      if (codeElement) {
        codeElement.style.color = '#64abd3';
      }
      const copyButton = document.createElement('button');
      copyButton.textContent = copyLabel;
      copyButton.className = 'copy-button';
      copyButton.setAttribute('aria-label', copyLabel);
      copyButton.addEventListener('click', async () => {
        const code = block.querySelector('code');
        const text = (code && code.textContent) || '';
        try {
          await navigator.clipboard.writeText(text);
          copyButton.textContent = copiedLabel;
          copyButton.classList.add('copied');
          setTimeout(() => {
            copyButton.textContent = copyLabel;
            copyButton.classList.remove('copied');
          }, 2000);
        } catch (err) {
          console.error('Copy error:', err);
        }
      });
      block.parentNode.insertBefore(copyButton, block.nextSibling);
    });
    const inlineCodes = document.querySelectorAll('article :not(pre) > code');
    inlineCodes.forEach((code) => {
      code.style.color = '#64abd3';
    });
  });
</script>

<style>
  article img { margin: 2rem auto; }
  article pre { overflow-x: auto; padding: 1.5rem; }
  article pre::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.5), transparent);
  }
  .prose pre code { font-size: 0.9em !important; background: transparent !important; border: none !important; padding: 0 !important; color: #64abd3 !important; }
  .prose :not(pre) > code { color: #64abd3 !important; }
  article pre code { font-size: 0.9em !important; background: transparent !important; border: none !important; padding: 0 !important; color: #64abd3 !important; }
  article :not(pre) > code { font-weight: 500; box-shadow: 0 0 0 1px rgba(16, 185, 129, 0.2); }
  article h2 { background: linear-gradient(90deg, rgba(59, 130, 246, 0.1), transparent); padding-left: 0.75rem; margin-left: -0.75rem; }
  article ul li, article ol li { padding-left: 0.5rem; }
  article ul li::marker { color: rgba(59, 130, 246, 0.7); }
  .copy-button {
    padding: 0.625rem 1rem;
    margin-top: -0.5rem;
    margin-bottom: 1.5rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 0.5rem;
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .copy-button:hover { background: rgba(255, 255, 255, 0.1); border-color: rgba(255, 255, 255, 0.3); color: rgba(255, 255, 255, 0.9); transform: translateX(4px); }
  .copy-button:active { transform: translateX(2px); }
  .copy-button.copied { background: rgba(16, 185, 129, 0.1); border-color: rgba(16, 185, 129, 0.3); color: rgba(16, 185, 129, 0.9); }
  .copy-button.copied:hover { background: rgba(16, 185, 129, 0.15); border-color: rgba(16, 185, 129, 0.4); }
</style>
